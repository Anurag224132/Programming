#include <iostream>
#include <cmath> //we have to include this header file for using these functions.
using namespace std;
int main()
{
    int x, y;
    cout << "Enter the value of x ";
    cin >> x;
    cout << "Enter the value of y ";
    cin >> y;
    cout << pow(x, y) << endl;
    cout << sqrt(x);
}